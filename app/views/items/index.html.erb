Items

<table>
  <tr>
    <th>Name</th>
    <th>Sa</th>
  </tr>
  <% @items.each do |item| %>
    <tr>
      <td><%= item.name %></td>
      <td><%= item.sa %></td>
      <td><%= link_to "Show", item %></td>
    </tr>
  <% end %>
</table>

